{
  "name": "ratatosk",
  "version": "0.8.44",
  "description": "Open-source browser extension that collects supplier invoices and receipts from vendor portals using the user's own session — no credential custody.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "npm run dev:collector",
    "dev:collector": "npm run gen:icons && vite --config collector/vite.config.ts",
    "dev:studio": "vite --config studio/vite.config.ts",
    "build": "npm run build:collector && npm run build:studio",
    "build:collector": "npm run gen:icons && vite build --config collector/vite.config.ts",
    "build:studio": "vite build --config studio/vite.config.ts",
    "package:collector": "tsx scripts/package-extension.ts collector",
    "package:studio": "tsx scripts/package-extension.ts studio",
    "assert:release-source": "tsx scripts/assert-release-source.ts",
    "release:collector": "npm run assert:release-source && npm run ci && npm run audit:security && npm run validate:collector-release && npm run build:collector && npm run assert:release-source && npm run package:collector",
    "release:studio": "npm run assert:release-source && npm run ci && npm run audit:security && npm run validate:studio-release && npm run build:studio && npm run assert:release-source && npm run package:studio",
    "gen:icons": "tsx scripts/build-icons.ts && tsx scripts/build-extension-icons.ts",
    "typecheck": "tsc -p tsconfig.core.json && tsc -p tsconfig.extension.json && tsc -p tsconfig.node.json && tsc -p tsconfig.test.json && tsc -p tsconfig.boundary-core.json && tsc -p tsconfig.boundary-extension.json && tsc -p tsconfig.boundary-node.json",
    "check:boundaries": "tsx scripts/check-architecture-boundaries.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "validate": "tsx scripts/validate-vendors.ts",
    "validate:release": "tsx scripts/validate-vendors.ts --release",
    "validate:collector-release": "npm run validate:release",
    "validate:studio-release": "npm run validate && tsx scripts/validate-studio-release.ts",
    "validate:pilot": "tsx scripts/validate-pilot-manifest.ts",
    "verify:collector-artifact": "tsx scripts/verify-collector-artifact.ts",
    "audit:security": "npm audit --audit-level=high",
    "ci": "npm run gen:icons && npm run typecheck && npm run check:boundaries && npm run validate && npm run test"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.28",
    "@types/chrome": "^0.0.268",
    "@types/node": "^20.19.43",
    "fflate": "^0.8.2",
    "simple-icons": "^16.26.0",
    "tsx": "^4.16.2",
    "typescript": "^5.5.4",
    "vite": "^8.1.4",
    "vitest": "^4.1.10"
  }
}
