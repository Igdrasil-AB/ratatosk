node_modules/
dist/
artifacts/
*.log
.DS_Store
.vite/
coverage/
.clawpatch/

# local skills tooling (npx skills add) — not part of the project
.claude/
.agents/
skills-lock.json

# Local UX review captures; final Web Store artwork lives in store/assets/.
store/ux-review/
# Fresh exact-artifact live acceptance is local release evidence, never source.
store/semantic-dom-acceptance.json
